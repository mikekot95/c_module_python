Сначала нам нужно собрать С-файл в Python-модуль, делаем это командой:
python setup.py install
Затем запускаем
python test.py
Тестирование: меняем значения в файле test.py. проверить флаг как true, так и false. Ограничения - на входе должно быть целое число от -2147483647 до 2147483646 (-2^31 + 1 и 2^31 - 2) включительно, иначе будет переполнение

Еще такой вопрос - 
если flag true возвращаем value++
если flag false возвращаем value—-
Это был вопрос с подвохом? Пост-инкремент же возвращает значение ДО его выполнения, и если бы я так и возвращал, значение бы просто не менялось и возрващался value. Поэтому я инкремент делал до того, как возвращать. Если же так и было задумано и нужно было возвращать просто value, надо переименовать module2.c в module.c, а старый куда-нибудь убрать, в общем сделал на всякий случай и так, и так.